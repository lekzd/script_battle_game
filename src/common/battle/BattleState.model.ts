
export enum BattleState {
    wait = 'waiting',
    ready = 'ready',
    battle = 'battle',
    results = 'results',
    win = 'win',
    lost = 'lost',
    attention = 'attention',
    connectionClosed = 'connectionClosed'
}